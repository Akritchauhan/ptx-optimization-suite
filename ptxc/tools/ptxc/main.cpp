#include <cstdio>
#include <string>

// Minimal stub — will be fully implemented in a later phase.
int main(int argc, char* argv[]) {
    std::printf("[ptxc] PTX Compiler frontend v0.1\n");
    std::printf("[ptxc] Use --help for usage information.\n");
    return 0;
}
